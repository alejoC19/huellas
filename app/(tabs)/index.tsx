import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function Home() {
  return (
    <ScreenPlaceholder
      title="Inicio"
      subtitle="Acá va el home con el nivel, el buscador y las huellas cerca (bloque 3)."
    />
  );
}
